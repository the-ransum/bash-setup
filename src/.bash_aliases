# .bash_aliases

# Set colors for ls and grep scripts
alias ls='ls --color=auto'
alias grep='grep --color=auto'
alias fgrep='fgrep --color=auto'
alias egrep='egrep --color=auto'

# Shortcut for common ls flags
alias ll='ls -l'
alias la='ll -a'

# Search history
alias search='history | grep'



